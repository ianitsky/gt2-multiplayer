disc/
generated/
bin/
obj/
settings.json
carda.sav
cardb.sav
interface.ini
packs/
ovl_bin/
__pycache__/
*.pyc
ovl_patched/
test_output*.txt
ram-*.bin
idx.txt

# Run output. Every one of these is written by a run and read by nobody
# else - ten of them had been committed before this rule existed, which is
# how a diff comes to be mostly last night's race.
*.log
captures/
out/
dist/
versions/

# Release output.
*.zip

# The relay this copy points at is a per-machine choice, and the address
# in it is somebody's own tunnel. config/relay.txt.example is the tracked
# one; copy it and put your address in the copy.
config/relay.txt
